# Usage

DotEM is a command-line tool that you can use to manage your environment configurations. The basic usage pattern is:

```bash copy
.em [command] [flags]
```
To get help on any command, you can use:

```bash copy
.em [command] --help
```