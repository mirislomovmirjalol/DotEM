# push Command

Description:

Push command designed to push environment from local environment file to DotEM.

Usage:

```bash copy
.em push [flags] <project_name> <file_path>
```
Examples:

```bash copy
.em push my_project /path/to/local/.env
```

Options:

-h, --help: Help for push command.