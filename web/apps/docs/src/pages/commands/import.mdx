# import Command

Description:

Import command is used to import all data from a file to DotEM storage.

Usage:

```bash copy
.em import [flags] <file_path>
```
Examples:

```bash copy
.em import /path/to/file.json
```
Options:

-h, --help: Help for import command.