# `add` command
The add command is designed to add a new project to DotEM storage with a given name and file path.

Usage:

```bash copy
.em add [flags] <project_name> <file_path>
```
Examples:

```bash copy
.em add my_project /path/to/project/.env
```