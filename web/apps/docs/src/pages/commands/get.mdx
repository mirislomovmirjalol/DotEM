# get Command

Description:
Get command designed to get a project by name from DotEM storage.

Usage:

```bash copy
.em get [flags] <project_name>
```
Examples:

```bash copy
.em get my_project
```

Options:

-h, --help: Help for get command.

Output Format:

```
env1=value1
env2=value2
```